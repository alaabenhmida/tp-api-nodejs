# ============================================
# Variables d'environnement - Généré par Ansible
# Ne pas modifier manuellement !
# Dernière mise à jour : {{ lookup('pipe', 'date -Iseconds') }}
# ============================================

PORT={{ app_port }}
NODE_ENV={{ node_env }}
{% if mongodb_uri is defined and mongodb_uri != '' %}
MONGODB_URI={{ mongodb_uri }}
{% endif %}